!!!
%html
	%head
		%title= "Faceshite | #{yield(:title)}"
		= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
		= javascript_include_tag "application", "data-turbolinks-track" => true
		= csrf_meta_tags
	%body
		%header
			#main_title faceshite
		%section
			#messages
				- flash.each do |key, value|
					.flash= value
			= yield
		%footer
			%p some footer text